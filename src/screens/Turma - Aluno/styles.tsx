import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "white",
    paddingVertical: 50,
  },
  contentContainer: {
    alignItems: "center",
    gap: 30,
    paddingBottom: 80,
  },
});
